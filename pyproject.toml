[tool.poetry]
name = "catrunner"
version = "1.0.0"
description = ""
authors = ["Ксения Травникова"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pygame = "^2.5.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
